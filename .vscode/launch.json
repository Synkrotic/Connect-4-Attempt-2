{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Current File",
            "type": "debugpy",
            "request": "launch",
            // "module": "Connect 4.main",
            "program": "${workspaceFolder}/main.py",
            "console": "integratedTerminal",
            // "cwd": "${workspaceFolder}"
        }
    ]
}